servers:
  - command: uvx
    name: mcp-server-fetch
    args: [ ]
  - command: npx
    name: "@modelcontextprotocol/server-filesystem"
    args:
      - "."
  - command: uvx
    name: mcp-server-sqlite
    args:
      - "--db-path"
      - "~/test.db"
